# QR Code Generator & Scanner
A simple Python tool to generate QR codes from URLs and scan them using your webcam.

## Features
- Generates QR codes and saves them locally.
- Real-time scanning using OpenCV.
- No external DLL dependencies (portable!).

## How to run
1. Install dependencies: `pip install -r requirements.txt`
2. Run the script: `python main.py`
